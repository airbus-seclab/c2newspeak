procedure t020 is
   type Enum1 is (a1,a2,a3);
   type Enum2 is (b1,a1,B3,B4);
   type Enum3 is (C1,C2,C3,C4,C5);
   A : Enum1 := a1;
   B : Enum2 := a1;
   C : Enum3 := c4;
begin
   if A1 <= Enum2'(A1) then
      A := A1;
   elsif A1 = B then
      B := B4;
      C := C5;
   end if;

end t020;
