with P2;
with P, A;
use P2;

package body P1 is

   procedure Piou (X:Integer) is
      Var : Enum;
   begin
      Var := G;
   end Piou;

   procedure Proc(Z : Integer) is
      X: Integer;
   begin

      if (A)
      then
         X := 2;
      end if;
      P(0);
      X:= P2.A;
   end proc;

end P1;
