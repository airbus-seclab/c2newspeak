--with Text_IO;
--use Text_IO;

With T098;
with T099, T096;
use T098;

package body T097 is

   procedure Piou (X:Integer) is
      Var : Enum;
   begin
      Var := G;
   end Piou;

   procedure Proc(Z : Integer) is
      X: Integer;
   begin

      if (A)
      then
--       Put ("yyyyyyyyyyyyyy");
         X := 2;
      end if;
      t099(0);
      X:= T098.A;
   end proc;

end T097;
